---
description: Triage the idea inbox - run every check, show what is half done, resolve what is finished
---

Run the `stickies:ideas` skill triage pass now.

Focus, if the user named one: "$ARGUMENTS"

Start by running the checks and the stale list, then walk the user through the results
in the order the skill defines. Lead with HALF DONE.
